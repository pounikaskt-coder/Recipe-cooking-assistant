# Rasoi — Recipe/Cooking Assistant Chatbot

Steps:
1. cd backend
2. python -m venv venv
3. venv\Scripts\activate (windows) / source venv/bin/activate (mac/linux)
4. pip install -r requirements.txt
5. .env.example copy pannitu .env nu rename pannunga, GEMINI_API_KEY podunga
6. python app.py
7. browser la http://127.0.0.1:5000 open pannunga